server:
  port: 7732

spring:
  application:
    name: member-service
  main:
    allow-bean-definition-overriding: true

eureka:
  instance:
    hostname: windows-test-member
    prefer-ip-address: true
  client:
    healthcheck:
      enabled: true
